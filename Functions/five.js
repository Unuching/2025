// arrow functions
// You don't need function name and return in arrow functions

let a = (x, y) => x * y;
console.log(a(9, 8));
// arrow functions are not hoisted
// arro functions do not have their own 'this'
